export const environment = {
  production: false,
  // Base URL of your FastAPI backend. Every path in
  // core/constants/api-endpoints.constant.ts is appended to this.
  // e.g. apiBaseUrl + '/login'  ->  http://localhost:8000/login
  apiBaseUrl: 'http://localhost:8000',
};
